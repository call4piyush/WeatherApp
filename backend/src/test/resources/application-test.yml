spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    
  h2:
    console:
      enabled: true

openweather:
  api:
    key: test-api-key
    base-url: http://localhost:8081/mock

logging:
  level:
    com.weatherapp: DEBUG 